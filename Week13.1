#include <stdio.h>
#include <string.h>

void dem(char s[])
{
    int i;
    int dem = 0;
    for (i = 0; i < strlen(s); i++)
    {
        if (s[i] == ' ')
        {
            dem++;
        }
    }
    printf("%d", dem);
}

int main()
{
    char s[100];
    gets(s);
    dem(s);
    return 0;
}
