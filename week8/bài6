//Sử dụng câu lệnh để in ra các số nguyên nhỏ hơn một số đứng trước.
#include <stdio.h>

int main ()
{
    int n;
    scanf("%d", &n);
    for (int i = 0;i<n;i++)
    {
        printf("%d ",i);
    }
    return 0;
}
