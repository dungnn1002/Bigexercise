//tính trung bình

#include <stdio.h>
#include<math.h>

int main ()
{
    int n, a[100];
    scanf("%d",&n);
    int sum =0;
    for(int i = 0;i<n;i++)
    {
        scanf("%d", &a[i]);
        sum += a[i];
        
    }
    float aver = sum/n;
    printf("%f", aver);
    return 0;
    
}
